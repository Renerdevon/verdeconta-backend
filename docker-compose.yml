version: "3.9"

services:
  postgres:
    image: postgres:16
    container_name: verdeconta_postgres
    restart: always
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: verdeconta
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
